import streamlit as st

st.set_page_config(page_title="MentorApp", layout="centered")

st.title("🌐 Урок 1: IP-адреса (Свои и Чужие)")

st.write("""
Представь, что **Росимущество** — это огромный бизнес-центр. 
- **Серый IP** — это номер твоего кабинета внутри здания (101, 102). 
- **Белый IP** — это адрес самого бизнес-центра (ул. Пушкина, 10).
""")

# Используем встроенный graphviz вместо mermaid
st.graphviz_chart('''
    digraph {
        rankdir=LR;
        node [shape=box, style=filled, color="#E1F5FE"];
        "Твой ПК\n(192.168.1.5)" -> "Роутер";
        "Коллега\n(192.168.1.10)" -> "Роутер";
        "Роутер" -> "INTERNET" [label="Белый IP: 213.180.x.x", color=blue];
        "INTERNET" -> "Сервер Росимущества";
    }
''')

st.divider()

st.subheader("🛠 Практика для руководителя:")
st.info("Завтра на работе проверь разницу между твоим 'внутренним' и 'внешним' адресом.")

col1, col2 = st.columns(2)
with col1:
    if st.button("Как узнать Серый IP?"):
        st.code("В консоли (cmd) введи:\nipconfig")
with col2:
    if st.button("Как узнать Белый IP?"):
        st.write("Загугли: **'мой ip'**")

st.sidebar.success("Прогресс: 20%")

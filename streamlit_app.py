import streamlit as st

st.set_page_config(page_title="MentorApp", layout="centered")

st.title("🌐 Урок 1: IP-адреса (Свои и Чужие)")

st.write("""
Представь, что **Росимущество** — это огромный бизнес-центр. 
- **Серый IP** — это номер твоего кабинета внутри здания (101, 102). Почтальон внутри здания тебя найдет, но из города в кабинет 101 письмо не дойдет без указания адреса самого здания.
- **Белый IP** — это адрес самого бизнес-центра (ул. Пушкина, 10). Его знает весь интернет.
""")

# Визуализация логики
st.mermaid("""
graph TD
    User1[Твой ПК: 192.168.1.5] --> Router(Роутер Компании)
    User2[Коллега: 192.168.1.10] --> Router
    Router -->|Внешний IP: 213.180.204.62| Internet((INTERNET))
    Internet --> Server[Сервер Росимущества]
""")

st.subheader("🛠 Практика для 'ленивого' руководителя:")
st.info("Завтра, сидя на работе с телефона или ПК, проверь две вещи:")

col1, col2 = st.columns(2)
with col1:
    if st.button("Как узнать мой 'Серый' IP?"):
        st.code("В консоли (cmd) введи: ipconfig\nИщи строку IPv4 (обычно 192.168.x.x или 10.x.x.x)")
with col2:
    if st.button("Как узнать мой 'Белый' IP?"):
        st.write("Просто забей в поиске: **'мой ip'**")

st.warning("⚠️ **ИБ-заметка:** 'Белый' IP виден всем. Если на нем открыты порты — это входная дверь для хакера.")

st.sidebar.success("Прогресс: 20%")

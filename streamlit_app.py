import streamlit as st

# 1. Делаем сайт ШИРОКИМ
st.set_page_config(page_title="MentorApp", layout="wide")

st.title("🔒 Урок 2: Порты и 'Замки' на сервере")

st.write("""
Если **IP-адрес** — это адрес дома, то **Порты** — это конкретные двери в этом доме. 
Некоторые двери должны быть открыты для гостей (сайт), а некоторые — заперты наглухо (база данных).
""")

# Делаем схему БОЛЬШЕ
st.graphviz_chart('''
    digraph {
        rankdir=TB;
        node [shape=component, style=filled, color="#FFF9C4", fontsize=20];
        "Сервер Росимущества (IP: 213.x.x.x)" -> "Порт 80 (HTTP)\nОТКРЫТ: Сайт работает" [color=green, penwidth=2];
        "Сервер Росимущества (IP: 213.x.x.x)" -> "Порт 22 (SSH)\nЗАКРЫТ: Вход для админов" [color=red];
        "Сервер Росимущества (IP: 213.x.x.x)" -> "Порт 3389 (RDP)\nВНИМАНИЕ: Удаленный стол" [color=orange];
    }
''', use_container_width=True)

st.divider()

# Интерактив для практики
st.subheader("🕵️‍♂️ Мини-симулятор хакера")
st.write("Попробуй 'простучать' порты нашего виртуального сервера:")

port = st.number_input("Введите номер порта для проверки (например: 80, 21, 3389)", value=80)

if st.button("Сканировать порт"):
    if port == 80:
        st.success(f"Порт {port} открыт. Это обычный веб-сайт. Хакеру тут скучно.")
    elif port == 3389:
        st.warning(f"Порт {port} найден! Это RDP. Если пароль '12345', сервер взломают за 5 минут.")
    else:
        st.error(f"Порт {port} закрыт или защищен Firewall. Сюда не пролезть.")

st.sidebar.header("Твой прогресс")
st.sidebar.progress(40)
st.sidebar.write("Уровень: Понимающий (Junior)")

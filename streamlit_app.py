import streamlit as st

# Настройка страницы
st.set_page_config(page_title="IT Ментор: Росимущество Edition", layout="wide")

st.title("🛡️ Твой персональный IT-Ментор")
st.write("Привет! Здесь мы будем прокачивать твои харды в Сетях и ИБ.")

# Боковое меню для навигации
st.sidebar.header("Твой прогресс")
st.sidebar.progress(10)
st.sidebar.write("Уровень: Новичок (Padawan)")

# Создаем вкладки
tab1, tab2, tab3 = st.tabs(["🌐 Сети", "🔒 ИБ", "📝 Мои заметки"])

with tab1:
    st.header("Как работает интернет?")
    st.info("Визуализация: Путь запроса от твоего ПК до сервера")
    
    # Схема в стиле Mermaid
    st.markdown("""
    ```mermaid
    graph LR
    A[Твой ПК] -->|1. Запрос| B(Роутер)
    B -->|2. Провайдер| C{DNS Сервер}
    C -->|3. IP адрес найден| B
    B -->|4. Летим по адресу| D[Сервер приложения]
    D -->|5. Ответ: Сайт| A
    ```
    """)
    st.write("---")
    st.subheader("Задание №1")
    st.write("Завтра на работе узнай, какой у твоего рабочего ПК внутренний IP-адрес. Обычно это делается командой `ipconfig` в консоли.")

with tab2:
    st.header("Основы Безопасности")
    st.warning("Правило №1: Никогда не доверяй входящим данным.")
    
    if st.button("Узнать главный секрет хакеров"):
        st.error("90% взломов — это не подбор пароля, а 'Социальная инженерия' (когда человек сам отдает пароль).")

with tab3:
    st.header("Твой словарик")
    st.text_area("Записывай сюда новые термины, которые услышишь на планерках:")
